namespace AdminToolsModels.LogicModels
{
    /// <summary>
    /// For different report types for every api service that can send a report
    /// </summary>
    public enum ReportType
    {
        Review,
        Comment,
        Discussion
    }
}
